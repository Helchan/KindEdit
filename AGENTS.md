# KindEdit Agent Development Rules

本文件用于约束和指导智能体在 KindEdit 项目中的开发行为。智能体必须优先遵守本文件规则；当本文件规则与临时任务目标存在冲突时，必须先向用户说明冲突并等待确认，不得自行绕过。

## 项目基本约束

- 当前项目是 Tauri 2 + React 18 + TypeScript + Rust 桌面应用。
- 编写代码前必须以 `package.json`、`package-lock.json`、`src-tauri/Cargo.toml`、`src-tauri/Cargo.lock` 和 `docs/KindEdit-软件开发规格说明.md` 中记录的当前技术版本基线为准；不得假设更高版本框架、编辑器内核、Tauri API 或 Rust 语言特性可用。
- 开发前必须先理解现有模块边界、数据流、状态管理方式、前后端职责划分和平台能力边界。
- 修改应优先沿用项目已有架构、编码风格、模块组织和技术栈，不得随意创建与现有体系冲突的新结构。
- 不得引入与当前项目规模、技术栈或架构方向不匹配的框架、状态库、构建工具或大型依赖。

## 变更范围与工作纪律

- 智能体必须围绕当前用户任务进行最小必要变更，不得顺手修改无关功能、无关样式、无关配置或无关依赖。
- 修改已有文件前，应先理解该文件当前职责和已有实现意图，不能用新实现直接覆盖未理解的旧逻辑。
- 如果发现与当前任务无关但确实重要的问题，应在交付说明中记录，不得擅自扩大任务范围。
- 如果用户明确给出 UI 文案、按钮顺序、交互流程、版本递增方式、构建方式或其他精确要求，必须按用户要求实现，不得自行重新解释。
- 不得删除、回滚或覆盖用户已有改动，除非用户明确要求。

## 跨平台要求

- 所有开发代码必须完美支持 Windows 和 macOS。
- 不得只基于单一平台行为实现功能；涉及窗口、文件路径、快捷键、系统菜单、滚动、输入法、字体渲染、文件对话框、权限、主题、打包等平台相关逻辑时，必须同时考虑 Windows 和 macOS。
- 平台差异必须显式处理，不能依赖偶然可用的默认行为。
- 任何会影响跨平台体验的改动，都必须说明 Windows 和 macOS 下的预期行为，并尽可能进行对应验证。
- 如果当前环境无法完成某个平台的真实验证，必须在交付说明中明确未验证平台、风险点和建议验证方式。

## 性能与交互要求

- 性能追求极致，UI 交互必须流畅。
- 编辑器输入、光标移动、滚动、选择、拖拽、标签切换、树视图展开折叠、Markdown 预览、文件打开保存等核心交互，不允许出现明显卡顿。
- 对大文件、复杂文档、频繁状态更新、高亮、解析、格式化、预览同步等场景，必须优先考虑性能成本。
- 不得在主交互路径中加入不必要的同步重计算、阻塞式 IO、全量渲染或高频全局状态更新。
- 功能实现不能只追求“能用”，必须兼顾响应速度、内存占用和连续操作体验。

## UI/UX 精确实现要求

- 用户明确指定的 UI 文案、按钮顺序、菜单层级、交互流程、快捷键、默认状态和视觉效果，必须精确实现，不得自行调整。
- 新增或修改 UI 时，不得破坏已有用户操作习惯、平台习惯和编辑器类软件的基础交互预期。
- 同一项目内必须保持整体 UI 风格一致；工具栏、状态栏、标签页、树视图、编辑区外围、设置界面、关于界面、右键菜单、弹窗、面板和对话框都必须遵循统一的视觉体系。
- 不得出现同一软件内不同界面在字体类型、字体大小、按钮尺寸、按钮样式、间距、圆角、阴影、颜色、标题层级、图标风格、控件密度等方面明显割裂。
- 新增或修改 UI 时，必须先对照项目现有主界面和同类界面，复用统一的设计变量、CSS 类、组件结构、控件样式和交互模式。
- 如果发现项目内已有界面风格不一致，应优先通过共享样式、公共组件或设计变量进行收敛，而不是继续复制各自独立的样式。
- 除非用户明确要求或存在清晰产品理由，不得为单个功能创建脱离整体风格的特殊 UI。
- 系统原生弹窗可以遵循操作系统原生风格；但项目自定义 UI 必须保持 KindEdit 内部风格一致。
- 编辑器输入、选择、撤销重做、复制粘贴、拖拽、滚动、焦点切换、快捷键冲突等交互必须优先保证稳定和可预期。
- 新增交互必须同时考虑键盘、鼠标、触控板、焦点状态、禁用状态、加载状态和错误状态。
- 涉及 Windows 和 macOS 差异的 UI 行为，必须遵循对应平台习惯；不能为了统一代码而牺牲平台体验。
- 不得使用只在单一平台、单一分辨率、单一主题或单一输入方式下成立的 UI 实现。

## 数据安全与文件安全要求

- KindEdit 是编辑器应用，所有文件打开、编辑、保存、另存为、关闭、会话恢复、配置持久化和自动恢复相关逻辑，必须优先保护用户数据安全。
- 不得在未获得用户明确确认的情况下丢弃、覆盖或破坏用户内容。
- 文件写入、覆盖、重命名、路径变更、权限失败、磁盘异常、编码异常、换行符差异和大文件处理必须有明确错误处理。
- 涉及保存、关闭、恢复会话或文件覆盖的改动，必须考虑异常路径，不能只验证成功路径。
- 文件路径处理必须同时考虑 Windows 和 macOS，包括路径分隔符、大小写敏感性、权限、特殊字符、空格、中文路径和系统保留路径。
- 对可能导致数据丢失的改动，必须在实现前明确风险，在实现后进行重点验证。

## 架构与代码质量总则

项目架构必须清晰、稳定、可扩展、可演进。代码实现必须服务于长期维护，不能只满足当前一次性需求。设计原则和设计模式是手段，不是目标；简单问题应保持简单实现，复杂问题才引入必要抽象。

## 架构与代码质量要求

- 新代码必须放在职责最匹配的位置，不能因为方便而堆入大型组件、全局 store、通用工具文件或后端入口文件。
- 代码必须以长期维护为目标，不能只满足当前一次性需求。
- 复杂逻辑必须拆分为职责清晰的小模块、小函数或小组件，但不得为了拆分而制造过多层级。
- 共享逻辑应抽取到合适的位置，避免重复实现；但只有在确实存在稳定复用价值时才抽象。
- 对核心编辑器、文件读写、文档解析、语法高亮、配置、会话恢复、平台适配等关键模块的改动，必须保持边界清晰、影响范围可控。
- 任何影响架构边界的改动，都必须在交付说明中解释改动位置、职责划分和扩展理由。

## 设计原则

- 代码必须尽量符合单一职责原则：一个模块、组件、函数或类型只承担清晰且有限的职责。
- 代码必须尽量符合开闭原则：新增能力时优先通过扩展已有抽象、注册机制、配置、组合或独立模块实现，避免频繁修改稳定核心逻辑。
- 代码应保持高内聚、低耦合：相关逻辑集中在合适模块内，不相关模块之间避免直接依赖。
- UI 表现、状态管理、业务逻辑、平台能力、文件 IO、解析格式化、配置/session 等职责必须保持清晰边界。
- 前端组件应优先负责展示和交互编排；复杂状态应放入合适的 store 或 hook；平台能力和文件系统能力应通过 Tauri command 或既有 API 边界访问。
- Rust 后端应优先承担文件 IO、文档类型处理、解析、格式化、配置持久化、会话持久化和平台相关能力，不应把可由前端清晰表达的纯 UI 状态强行下沉。
- 类型定义应准确表达业务含义，避免使用过宽的 `any`、松散字符串或含义不明的数据结构。
- 错误处理应在合适层级完成，不能吞掉会影响用户判断或数据安全的错误。

## 设计模式与反过度设计

- 可以适当使用设计模式，但必须服务于实际复杂度。
- 不得为了使用设计模式而使用设计模式。
- 当代码存在明确的变化点、扩展点或重复结构时，可以引入合适的模式，例如注册表、策略、适配器、工厂、观察者、命令等。
- 本项目已有类似注册表的结构时，应优先沿用，例如文档类型、语法高亮、解析格式化等扩展点。
- 引入设计模式或新抽象后，必须让代码更清晰、更稳定或更容易扩展；如果只增加理解成本，则不应引入。
- 不得提前为尚不存在的需求设计庞大抽象。
- 不得创建空泛的 base class、manager、helper、service、utils 等模块来容纳不明确职责。
- 不得为了“未来可能需要”而增加当前没有实际使用场景的配置、接口、泛型、插件层或事件系统。
- 每一次抽象都必须有明确理由：减少重复、隔离变化、降低耦合、提升可测试性或匹配已有架构。
- 如果无法说明某个抽象解决了什么真实问题，就不应引入该抽象。

## 依赖管理要求

- 不得轻易新增 npm、Rust 或系统级依赖。
- 新增依赖前必须先确认现有依赖、标准库或项目内已有能力无法合理满足需求。
- 新增依赖必须说明必要性、替代方案、维护风险、体积影响、性能影响和跨平台影响。
- 不得为了少量简单逻辑引入大型依赖。
- 不得引入长期无人维护、许可证不清晰、平台支持不明确或与项目技术栈冲突的依赖。
- 删除或替换依赖前，必须确认项目中没有其他模块仍在使用，并验证构建链路不受影响。
- 依赖版本变更必须同步更新锁文件，并在交付说明中说明原因。
- 涉及 Tauri、React、Monaco Editor、Milkdown、Rust edition、TypeScript、Vite 或其他核心依赖的代码实现时，必须先确认当前声明版本和锁定版本支持所使用的 API、配置项和语言特性；如果需要依赖更高版本能力，必须先说明升级必要性、影响范围和验证方式，并同步维护规格文档。

## 可演进性要求

- 新增功能时，应考虑未来同类功能如何接入。
- 涉及文档类型、语法高亮、解析、格式化、编辑器能力、主题、配置、平台差异等扩展点时，应优先设计为可注册、可替换或可组合的结构。
- 稳定核心逻辑应保持小而可靠，变化频繁的逻辑应隔离到独立模块。
- 新增文档类型时，应同时考虑前端语言映射、编辑器行为、后端文档处理、语法高亮、树结构解析和格式化能力之间的边界。
- 新增 UI 能力时，应考虑组件复用、状态归属、键盘交互、主题适配和平台差异。
- 新增配置项时，应考虑默认值、持久化、兼容旧配置、UI 设置入口和会话恢复影响。

## 持续演进与必要重构

- 项目经过多轮开发后，原有设计可能不再适合当前功能规模和复杂度。智能体在开发过程中必须主动识别这类架构退化问题。
- 当新增功能、修复问题或优化性能时，如果发现现有结构已经明显阻碍实现、扩展、维护或性能优化，应提出并执行适当重构。
- 重构必须服务于明确目标，例如理清职责边界、减少重复代码、隔离变化点、改善性能、提升可测试性、降低耦合或恢复架构一致性。
- 不得为了“顺手整理”“个人偏好”或“看起来更优雅”进行无目标重构。
- 重构范围必须与当前任务相关，优先进行小步、局部、可验证的重构。
- 不得在没有必要性的情况下重写稳定模块、替换核心架构、迁移技术栈或大规模调整目录结构。
- 如果确实需要较大范围重构，必须先说明现有问题、重构目标、影响范围、风险点和验证方式，并等待用户确认。
- 重构后必须保持原有用户可见行为不被破坏，除非任务明确要求改变行为。
- 重构后必须进行必要验证，确保功能、性能、跨平台行为和数据安全没有回退。
- 如果当前任务暴露出架构问题但不适合立即重构，应在交付说明中明确记录该问题和后续建议，不能假装问题不存在。

## 规格文档维护要求

- 智能体在每次功能变更或用户可见行为变更后，必须同步维护 `docs/KindEdit-软件开发规格说明.md`。
- 功能新增、功能删除、交互调整、快捷键变化、菜单变化、配置变化、文件处理变化、文档类型支持变化、解析/格式化/高亮行为变化、构建打包行为变化、跨平台行为变化，都属于必须更新规格文档的范围。
- 规格文档必须描述当前真实行为，不能保留已经失效的旧设计、旧限制或旧流程。
- 如果实现结果与原规格不一致，必须以实际确认后的产品行为为准更新规格文档。
- 如果 `docs/KindEdit-软件开发规格说明.md` 不存在，首次需要维护规格时必须创建该文件，并写入与当前功能相关的规格内容。
- 不得只修改代码而遗漏对应规格文档更新，除非该任务完全不涉及功能、行为、构建、配置或用户体验变化。
- 交付说明中必须明确是否更新了规格文档；如果未更新，必须说明原因。

## 问题记录要求

- 重大问题、重复出现的问题、复杂修复、跨平台问题、性能问题、数据安全问题和构建打包问题，必须记录到 `docs/问题记录与解决方案.md`。
- 问题记录应包含问题现象、触发条件、原因分析、解决方案、影响范围和验证方式。
- 如果 `docs/问题记录与解决方案.md` 不存在，首次需要记录问题时必须创建该文件。
- 普通小修、纯文案调整、无复用价值的一次性问题可以不记录，避免问题文档噪音。
- 如果当前任务暴露出重要问题但暂不适合修复，必须在问题记录或交付说明中明确后续处理建议。

## 版本号递增规则

- 项目版本号由三部分组成，格式为 `大版本号.中版本号.小版本号`。
- 当用户要求递增大版本号时，只递增第一段大版本号。
- 当用户要求递增中版本号时，只递增第二段中版本号。
- 当用户要求递增小版本号时，只递增第三段小版本号。
- 不得在用户只要求递增某一段版本号时，同时修改其他段版本号。
- 不得自行切换为其他版本策略，例如语义化版本中的自动归零规则，除非用户明确要求。
- 大版本号不自动递增，只有用户明确要求递增大版本号时才允许修改第一段版本号。
- 进行问题修复或优化时，如果用户没有指定版本号递增方式，默认自动递增小版本号。
- 开发新功能时，如果用户没有指定版本号递增方式，默认自动递增中版本号。
- 版本号修改必须同步到项目中所有实际参与构建、打包、显示或发布的版本声明位置，避免不同文件中的版本号不一致。
- 当前项目至少必须同步检查并更新 `package.json`、`src-tauri/Cargo.toml`、`src-tauri/tauri.conf.json` 中的版本号。

## 编译、构建与打包要求

- 修复、优化或新功能开发完成后，必须执行项目根目录的 `build_package.command` 进行构建打包。
- `build_package.command` 是项目官方构建打包入口；编译、构建、打包为 macOS `.app` 或 `.dmg` 时，必须直接使用该脚本实施。
- 如果后续需要生成 Windows 安装包或其他平台产物，也应优先通过 `build_package.command` 实施；如果脚本尚不支持目标产物，必须先优化修复该脚本，再通过该脚本实施。
- 不得绕过 `build_package.command` 手工执行零散构建、复制、打包或生成 `.dmg` 的命令，除非用户明确要求临时诊断。
- 如果 `build_package.command` 无法满足当前编译、构建、打包、产物命名、产物路径、清理、跨平台兼容或错误处理要求，必须优先优化修复该脚本。
- 修复 `build_package.command` 后，必须再次使用该脚本完成实际构建或打包验证。
- 构建脚本应作为项目官方构建入口持续维护，后续构建流程变化应沉淀到该脚本中，不能只停留在聊天记录或一次性命令中。
- 如果当前系统环境无法执行 `build_package.command` 或无法生成目标平台产物，不能绕过脚本伪造结果，必须在交付说明中明确失败原因、缺失环境、影响范围和下一步处理方式。
- 执行打包后，交付说明必须明确实际使用的命令、生成的 `.app` 或 `.dmg` 路径，以及是否完成清理构建。

## 验证要求

- 完成代码修改后，必须执行与变更范围匹配的验证；不得只凭静态阅读判断完成。
- 涉及前端、类型、构建链路或 UI 行为的改动，必须至少执行能够覆盖 TypeScript 与前端构建的验证。
- 涉及 Rust、Tauri command、文件 IO、配置、会话、文档处理、解析、格式化或打包链路的改动，必须至少执行能够覆盖 Rust/Tauri 构建链路的验证。
- 涉及 UI 交互、编辑器体验、窗口行为、文件对话框、快捷键、主题或平台差异的改动，应进行真实运行或可视化验证；无法验证时必须说明风险。
- 验证失败时，必须优先修复失败原因；如果因环境限制无法修复或无法继续验证，必须明确说明。

## 默认验证命令

- 前端、TypeScript、UI 或通用代码改动后，默认执行 `npm run build`。
- Rust、Tauri command、文件 IO、文档处理、配置、会话或后端逻辑改动后，默认执行 `cargo check --manifest-path src-tauri/Cargo.toml`。
- 修复、优化或新功能开发完成后，按构建打包规则执行 `./build_package.command`。
- 如果构建打包需要清理构建产物或排查缓存问题，优先执行 `./build_package.command --clean`。
- 如果未来项目新增测试脚本，必须把对应测试命令补充到本章节，并在相关改动后执行。
- 如果默认验证命令因环境限制无法执行，必须在交付说明中明确失败命令、失败原因和风险。

## Git 与版本交付规则

- 默认交付分支是 `main` 分支。
- `commit` 和 `push` 必须由用户明确提出后才允许执行；智能体不得根据任务完成状态自行决定提交或推送。
- 只有用户明确要求“提交”或 `commit` 时，智能体才允许执行 `git add` 和 `git commit`。
- 只有用户明确要求 `push` 或“推送”时，智能体才允许执行 `git push`。
- 禁止未经用户明确指令直接提交、推送、打 tag、创建 release 或修改远程分支。
- 提交前必须检查 `git status` 和相关 diff，只能暂存与当前任务相关的文件。
- 不得把无关改动、临时文件、缓存文件、构建中间产物或未确认的大型产物加入提交。
- 如果用户要求提交并推送，必须提交到默认分支 `main` 并推送到对应远程分支，除非用户明确指定其他分支。
- 推送必须使用 SSH 方式；如果当前远程仓库不是 SSH URL，必须先向用户说明并等待确认，不得自行改用 HTTPS 推送。
- 版本递增、规格文档、问题记录和构建脚本相关改动，应在用户明确要求提交时与对应功能改动一起提交。

## 交付闭环

- 完成问题修复、优化或新功能开发后，必须先同步必要文档，再按规则递增版本号，然后执行必要验证和构建打包脚本。
- 交付说明必须列出关键改动、规格文档是否更新、版本号是否递增、构建打包命令、生成产物路径、已验证内容和未验证风险。
- 如果某项必需步骤因环境或权限无法完成，必须明确说明原因、影响和下一步处理方式。
